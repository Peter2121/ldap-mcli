# LDAP Go Library

A golang library for connection to and interacting with E-Mail LDAP server (like iRedMail LDAP server).
Based on https://github.com/atselvan/ldap-go-lib

In development, don't use in production.
